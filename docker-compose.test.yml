services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: drivly_test
      POSTGRES_PASSWORD: drivly_test
      POSTGRES_DB: drivly_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
